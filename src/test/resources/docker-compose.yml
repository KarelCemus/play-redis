version: '3.7'
services:
  redis-master:
    image: redis:latest
    hostname: redis-master
    ports:
      - '6379:6379'

  redis-slave:
    image: redis:latest
    hostname: redis-slave
    ports:
      - '6479:6379'
    command: redis-server --slaveof redis-master 6379
