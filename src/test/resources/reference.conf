# ==================
# Akka configuration
# ==================

akka {
  log-dead-letters = off
  log-dead-letters-during-shutdown = off

  actor {
    # disables warning
    warn-about-java-serializer-usage = off
  }
}
